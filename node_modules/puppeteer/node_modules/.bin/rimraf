#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_rimraf@3.0.2@rimraf/bin.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../_rimraf@3.0.2@rimraf/bin.js" "$@"
  ret=$?
fi
exit $ret

#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_extract-zip@2.0.1@extract-zip/cli.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../_extract-zip@2.0.1@extract-zip/cli.js" "$@"
  ret=$?
fi
exit $ret
